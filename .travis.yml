language: node_js # Node runs `npm install` and `npm test` automatically
node_js:
  - 6 # via nvm
sudo: false # Allows Travis to run this project in container
branches:
  only:
    - gh-pages # otherwise branch is ignored
    - /^greenkeeper-.*$/
